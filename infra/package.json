{
  "name": "@white-glove/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "diff": "cdk diff",
    "test": "echo \"No infra unit tests\""
  },

  "dependencies": {
    "aws-cdk-lib": "^2.173.2",
    "constructs": "^10.4.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "aws-cdk": "^2.173.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
